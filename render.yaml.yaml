services:
  - type: web
    name: pagina-ps5
    env: docker
    plan: free
    dockerfilePath: Dockerfile.txt
    buildCommand: "php artisan config:clear && php artisan cache:clear && php artisan view:clear"